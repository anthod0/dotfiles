#!/bin/sh
set -eu

command -v bun >/dev/null 2>&1 || { echo "bun is required to sync Codex config." >&2; exit 1; }
exec bun {{ joinPath .chezmoi.sourceDir "scripts/sync-codex-config.ts" | shellQuote }}
